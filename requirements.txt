# --- NEW: RunPod / Docker dependencies ---
# PyTorch and CUDA are provided by the base image (keep these in sync)
# Core Frameworks
lerobot
gym-aloha
diffusers
opencv-python
matplotlib
tqdm
numpy==1.26.4
av
# Add any other libraries used in aloha_train.py here
